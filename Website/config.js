/* Capsi download configuration — change in one place. */
const CAPSI_CONFIG = {
  // Direct download for the Windows executable (GitHub latest release asset).
  // To retarget (new owner/repo/asset name), edit this line only.
  DOWNLOAD_URL: "https://github.com/mystrdan/Capsi/releases/latest/download/capsi.exe",
  X_URL: "https://x.com/runcapsi",
  SITE_URL: "https://capsi.win",
  VERSION_LABEL: "Windows"
};
